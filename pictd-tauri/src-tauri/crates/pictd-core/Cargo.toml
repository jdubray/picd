[package]
name = "pictd-core"
version = "0.1.0"
edition = "2021"

[dependencies]
arboard = { version = "3", features = ["image-data"] }
image = "0.25"
chrono = "0.4"
dirs = "5"
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
regex = "1"
